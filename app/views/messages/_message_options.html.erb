<fieldset>
  <legend>Sending options</legend>
  <div class="field">
    <%= f.label :route %>:
    <%= f.select :route_id,
      options_from_collection_for_select(current_user.routes, "id", "name")
    %>
  </div>
</fieldset>

