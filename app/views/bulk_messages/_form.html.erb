<%= form_for(@bulk_message) do |f| %>
  <% if @bulk_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bulk_message.errors.count, "error") %> prohibited this bulk_message from being saved:</h2>

      <ul>
      <% @bulk_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <legend>Lists</legend>
    <%= select_tag :lists,
      options_from_collection_for_select(current_user.lists, "id", "name"),
      {:multiple => true, :size => 5}
    %>
  </fieldset>

  <%= render '/messages/message_text', :f => f %>

  <%= render '/messages/message_options' %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
